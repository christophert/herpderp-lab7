#ifndef VERIFY
#define VERIFY

#include <string>

using namespace std;

int verifyCC(string ccno);
int verifyDate(string datestr);
int verifyZIP(string zip);
int verifyEmail(string email);
int verifyPurchAmt(string amt);

#endif
